# Decor Sprites

Place your decorative PNG sprites here.

Naming convention:
- The file name becomes the sprite key: `decor:<name>`
- Example: `flower.png` -> `decor:flower`

These sprites are preloaded by Pixi (see `src/svgDecorHelper.ts`).
